[config]=
DEFAULT_REGION = us-central1-a
DEFAULT_PROJECT = segmind-base
DEFAULT_FAMILY = segmind-fam

[url]=
GCP_URL = https://compute.googleapis.com/compute/beta/projects/<PROJECT_ID>/global/machineImages
